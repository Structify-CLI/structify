{
    "name": "structify",
    "private": true,
    "version": "1.0.5",
    "workspaces": [
        "packages/*",
        "templates/*"
    ],
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm --filter @structify/cli dev",
        "release": "npm version patch && git push --follow-tags"
    }
}
